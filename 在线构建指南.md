# 🌐 在线APK构建指南

## 问题描述
本地编译时遇到网络连接问题，无法下载Gradle依赖。

## 🚀 解决方案：使用在线构建服务

### 方案A：GitHub Actions构建

#### 步骤1：上传代码到GitHub
```bash
# 创建GitHub仓库并上传项目
git init
git add .
git commit -m "陌陌AI自动回复系统"
git remote add origin https://github.com/yourusername/momo-auto-reply.git
git push -u origin main
```

#### 步骤2：配置GitHub Actions
创建 `.github/workflows/build.yml`:
```yaml
name: Build Android APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
    
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    
    - name: Build with Gradle
      run: ./gradlew assembleDebug
    
    - name: Upload APK
      uses: actions/upload-artifact@v3
      with:
        name: app-debug
        path: app/build/outputs/apk/debug/app-debug.apk
```

### 方案B：使用在线APK构建网站

#### 推荐网站：
1. **ApkOnline** (https://www.apkonline.net/)
2. **MIT App Inventor** (http://appinventor.mit.edu/)
3. **Appy Pie** (https://www.appypie.com/)
4. **Build APK Online** (https://buildapk.online/)

#### 使用步骤：
1. 上传项目文件
2. 选择构建选项
3. 等待构建完成
4. 下载生成的APK

### 方案C：使用云端开发环境

#### 推荐平台：
1. **Codespaces** (GitHub Codespaces)
2. **Replit** (https://replit.com/)
3. **CodeSandbox** (https://codesandbox.io/)
4. **Gitpod** (https://gitpod.io/)

#### 使用GitHub Codespaces示例：
1. 在GitHub上Fork此项目
2. 点击"Code" → "Codespaces" → "Create codespace"
3. 在云端运行编译命令
4. 下载APK文件

## 📱 真机测试完整流程

### 第一步：获取APK文件
选择上述任一方案获取 `app-debug.apk` 文件

### 第二步：传输到手机
```bash
# 方法1：数据线传输
adb push app-debug.apk /sdcard/

# 方法2：网盘传输
# 上传到百度云、阿里云等，然后手机下载

# 方法3：直接下载
# 如果有在线构建链接，手机浏览器直接下载
```

### 第三步：手机安装
```bash
# 启用开发者模式
设置 → 关于手机 → 连续点击版本号7次
设置 → 开发者选项 → 开启USB调试

# 安装APK
adb install app-debug.apk

# 或在手机文件管理器中点击APK文件安装
```

### 第四步：权限配置
1. **网络权限**：首次启动时允许
2. **存储权限**：允许访问存储
3. **无障碍服务**：
   ```
   设置 → 无障碍 → 陌陌AI自动回复 → 开启
   ```

## 🔧 替代方案：免编译版本

如果上述方案都不可行，我们可以创建：

### 方案1：APK模板文件
创建包含所有必要配置的空APK，然后手动添加功能

### 方案2：使用第三方开发平台
- **AppGyver** (SAP Build Apps)
- **Flutter Flow**
- **Adalo**
- **Glide**

### 方案3：云端编译服务
- **CircleCI**
- **Travis CI**
- **GitLab CI**

## 📞 技术支持

如果遇到构建问题：
1. 检查项目文件完整性
2. 验证配置文件正确性
3. 查看构建日志错误信息
4. 尝试不同的构建平台

## ⚠️ 注意事项

1. **安全性**：确保APK来源可信
2. **兼容性**：检查手机Android版本
3. **权限**：务必开启无障碍服务
4. **测试**：建议先在测试环境验证

---

**建议优先尝试GitHub Actions构建，自动化程度高且稳定。**